- Extend coverage to additional custom JavaScript widgets if a supported
  widget does not inherit the standard date or datetime behavior.
- Review export, email template, and external API formatting use cases
  separately because this module intentionally keeps stored values and search
  domains in CE.
- Keep manual input correction limited to 4-digit years unless a future design
  explicitly defines safe 2-digit year handling.
